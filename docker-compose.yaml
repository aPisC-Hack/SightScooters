version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: ./workspaces/api/Dockerfile
  proxy:
    build:
      context: .
      dockerfile: ./workspaces/proxy/Dockerfile
    ports:
      - 80:80